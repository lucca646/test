---
description: Secrets EAS / TestFlight pour builds iOS non-interactifs
globs: mobile/**
alwaysApply: false
---

# EAS TestFlight — secrets locaux

Fichier **gitignoré** : `mobile/.env.eas` (modèle : `mobile/.env.eas.example`).

Variables dans `mobile/.env.eas` (gitignoré) :
- `EXPO_TOKEN` — token perso Expo (agent peut le (re)créer via GraphQL + session)
- `EXPO_APPLE_ID` — `rouxellucca@gmail.com`
- `EXPO_APPLE_TEAM_ID` — `5JUVV8Y56D`
- `EXPO_ASC_APP_ID` — `6797030593`
- Submit : API Key ASC déjà sur serveurs EAS (`MG8MM8LD42`) — pas de MDP d’app

Quand l’utilisateur demande un build TestFlight :
1. `./scripts-eas-check-secrets.sh`
2. `./scripts-eas-testflight.sh` (production + auto-submit, non-interactif)

Ne jamais committer `.env.eas`, `*.p8`, ni tokens.
