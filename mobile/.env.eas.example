# Copier vers .env.eas et remplir (NE PAS committer .env.eas)
#   cp .env.eas.example .env.eas

# ── Expo (obligatoire pour builds non-interactifs) ──────────────────────────
# https://expo.dev → Account → Access tokens → Create token
EXPO_TOKEN=

# ── Apple ───────────────────────────────────────────────────────────────────
EXPO_APPLE_ID=rouxellucca@gmail.com
EXPO_APPLE_TEAM_ID=5JUVV8Y56D
EXPO_ASC_APP_ID=6797030593

# Optionnel si l’API Key ASC est déjà sur les serveurs EAS (cas actuel) :
# EXPO_APPLE_APP_SPECIFIC_PASSWORD=

# ── API Messages (CRM SMS COR·ALT) ─────────────────────────────────────────
# Token serveur MCP_API_TOKEN (evolution-api/.env) — inliné dans le bundle JS
# à l'export EAS Update/Build. NE JAMAIS committer la valeur réelle
# (ce repo est public) : uniquement dans .env.eas (gitignoré).
EXPO_PUBLIC_MESSAGES_API_TOKEN=
