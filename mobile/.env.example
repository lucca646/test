# API COR·ALT
EXPO_PUBLIC_API_URL=https://cal.coraia.eu
EXPO_PUBLIC_APP_ENV=development

# Bridge session (Expo Go) — démarrer: npm run bridge
# Puis tunnel Cloudflare / ngrok vers le port 8791
EXPO_PUBLIC_BRIDGE_URL=

# API Messages (CRM SMS COR·ALT — imessage-server.ts, nginx :8008)
EXPO_PUBLIC_MESSAGES_API_URL=http://46.62.139.238:8008
# Token serveur (MCP_API_TOKEN côté evolution-api/.env) — auth Bearer,
# pas de mot de passe utilisateur pour l'instant.
EXPO_PUBLIC_MESSAGES_API_TOKEN=
