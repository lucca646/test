import TabScreen from "../components/TabScreen";

export default function GamesTab() {
  return (
    <TabScreen
      title="Jeux"
      body="Icône SF Symbol rocket — fournie par le système iOS."
      tint={["#fb923c", "#ea580c"]}
    />
  );
}
