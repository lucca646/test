# Référence COR·ALT (copie locale)

Copies en lecture seule depuis `/root/alternance` pour travailler dans ce repo
sans modifier COR-ALT.

- `api/` — modules `frontend/src/api/*`
- `utils/` — planAccess, accountActivation, bool
- `DEV_PROD.md` — instances prod/dev

L’app runtime est [`../../coralt-mobile`](../../coralt-mobile).
